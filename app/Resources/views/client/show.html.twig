{% extends 'base.html.twig' %}

{% block content %}
    <h1>Customer n° {{client.id}} </h1>

    <table>
        <tbody>
            <tr>
                <th>Company</th>
                <td>{{ client.company }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ client.mail }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ client.adress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ client.city }}</td>
            </tr>
            <tr>
                <th>Phone number</th>
                <td>{{ client.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('client_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('client_edit', { 'id': client.id }) }}">Edit</a>
        </li>
    </ul>
{% endblock %}
